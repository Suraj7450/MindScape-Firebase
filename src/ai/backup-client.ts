// src/ai/backup-client.ts

export async function generateWithBackup(...args: any[]) {
    throw new Error(
        'Backup AI client not configured. Remove backup-client import or implement it.'
    )
}
